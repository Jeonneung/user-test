ports:
  - port: 8080
    onOpen: ignore
    visibility: public
    description: User Service API

tasks:
  - name: Start Docker Compose
    command: |
      cd User
      docker-compose up -d